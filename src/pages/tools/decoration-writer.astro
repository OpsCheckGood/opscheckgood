---
import Base from '../../layouts/Base.astro';
import DecorationWriter from '../../components/DecorationWriter.tsx';
---

<Base
  title="Decoration Writer — Ops Check Good"
  description="Draft a decoration citation that fits the myDecs certificate: wrapped as the certificate wraps it, held to the lines it prints, with the manual's opening and closing sentences. Runs entirely in your browser."
  brand="Decoration Writer"
  tagline="Fit the certificate, not the counter."
>
  <DecorationWriter client:load />
</Base>
